<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Form View للشحنة -->
    <record id="view_shipment_order_form" model="ir.ui.view">
        <field name="name">shipment.order.form</field>
        <field name="model">shipment.order</field>
        <field name="arch" type="xml">
            <form string="Shipment Order">
                <header>
                    <button name="action_confirm"
                            string="✓ Confirm"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>

                    <!-- Confirmed → Picked -->


                    <!-- Picked → Torood Hub -->
                    <button name="action_to_torood_hub"
                            string="🏢 To Torood Hub"
                            type="object"
                            class="btn-warning"
                            help="Mark shipment as arrived at Torood Hub"
                            invisible="state != 'picked'"/>

                    <button name="action_pickup"
                            string="📦 Pickup"
                            type="object"
                            class="btn-info"
                            invisible="state != 'confirmed'"/>

                    <!-- Torood Hub → In Transit -->
                    <button name="action_in_transit"
                            string="🚛 In Transit"
                            type="object"
                            class="btn-info"
                            invisible="state != 'torood_hub'"
                            help="Shipment leaving Torood Hub"/>

                    <!-- In Transit → Shipping Company Hub -->
                    <button name="action_to_shipping_company_hub"
                            string="🏭 To Shipping Hub"
                            type="object"
                            class="btn-primary"
                            invisible="state != 'in_transit'"
                            help="Arrived at Shipping Company Hub"/>

                    <!-- Shipping Company Hub → Out for Delivery -->
                    <button name="action_out_for_delivery"
                            string="📍 Out for Delivery"
                            type="object"
                            class="btn-warning"
                            invisible="state != 'shipping_company_hub'"/>

                    <!-- Out for Delivery → Delivered -->
                    <button name="action_deliver"
                            string="✅ Delivered"
                            type="object"
                            class="btn-success"
                            invisible="state != 'out_for_delivery'"/>

                    <!-- Return Button -->
                    <button name="action_return"
                            string="❌ Return"
                            type="object"
                            class="btn-danger"
                            invisible="state != 'out_for_delivery'"/>

                    <!-- Cancel Button -->
                    <button name="action_cancel"
                            string="Cancel"
                            type="object"
                            class="btn-secondary"
                            invisible="state not in ['draft', 'confirmed']"/>

                    <!-- Reset to Draft Button -->
                    <button name="action_reset_to_draft"
                            string="🔄 Reset to Draft"
                            type="object"
                            class="btn-warning"
                            invisible="state in ['draft', 'delivered']"
                            groups="base.group_system"/>

                    <button name="action_create_vendor_bill"
                            string="Create Vendor Bill"
                            type="object"
                            class="btn-warning"
                            invisible="state in ['draft', 'cancelled'] or vendor_bill_status == 'billed'"
                            confirm="Are you sure you want to create a vendor bill for this shipment?"
                            />
                    <field name="state" widget="statusbar" 
                             statusbar_visible="draft,confirmed,torood_hub,picked,in_transit,shipping_company_hub,out_for_delivery,delivered"/>

                </header>

                <div class="alert alert-danger text-center"
                     invisible="state != 'returned'"
                     role="alert">
                    <h4>
                        <i class="fa fa-exclamation-triangle"/> This Shipment Has Been Returned
                        <span invisible="is_return_processed">
                            - Penalties Not Yet Applied
                        </span>
                        <span invisible="not is_return_processed">
                            - Penalties Applied (Check Invoices)
                        </span>
                    </h4>
                </div>
                <sheet>
                    <!-- Smart Buttons -->
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" type="object" 
                                name="action_view_invoices" icon="fa-file-text-o"
                                invisible="state in ['draft','cancelled']">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_value">
                                    <field name="invoice_count"/>
                                </span>
                                <span class="o_stat_text">Customer Invoices</span>
                            </div>
                        </button>


                        <button class="oe_stat_button" type="object"
                                name="action_view_vendor_bills" icon="fa-file-text-o"
                                invisible="state in ['draft','cancelled']">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_value">
                                    <field name="vendor_bill_count"/>
                                </span>
                                <span class="o_stat_text">Vendor Bills</span>
                            </div>
                        </button>

                        <button class="oe_stat_button"
                                invisible="not tracking_number"
                                icon="fa-barcode">
                            <div class="o_field_widget o_stat_info">
                                <span class="o_stat_text">Tracking</span>
                                <span class="o_stat_value text-truncate">
                                    <field name="tracking_number" class="text-primary"/>
                                </span>
                            </div>
                        </button>
                    </div>
                    
                    <widget name="web_ribbon" title="Cancelled" 
                            invisible="state != 'cancelled'"
                            bg_color="text-bg-danger"/>
                    <widget name="web_ribbon" title="Delivered" 
                            invisible="state != 'delivered'"
                            bg_color="text-bg-success"/>
                    <widget name="web_ribbon" title="Returned" 
                            invisible="state != 'returned'"
                            bg_color="text-bg-warning"/>
                    
                    <div class="oe_title">
                        <h1>
                            <field name="order_number" readonly="1"/>
                        </h1>

                        <group string="Important to export sheet Excel">
                            <div class="o_row">
                            <label for="allow_the_package_to_be_opened" string="📦 Whether to allow the package to be opened"/>
                            <field name="allow_the_package_to_be_opened" widget="boolean_toggle" nolabel="1"/>
                        </div>
                            <field name="delivery_type"

                                   widget="radio"
                                   options="{'horizontal': True}"/>
                        </group>

                    </div>
                    
                    <group>
                        <group string="Shipping Details">
                            <field name="shipment_type"/>
                            <field name="pickup_date"/>
                            <field name="expected_delivery"/>
                            <field name="actual_delivery" readonly="1" invisible="state != 'delivered'"/>
                             <field name="tracking_number" invisible="state in ['draft']"
                                   class="fw-bold text-primary"/>
                            <field name="tracking_url" widget="url" invisible="not tracking_number"
                                   string="Track Shipment"/>

                            <field name="tracking_number_shipping_company" invisible="state in ['draft']"
                                   class="fw-bold text-primary"/>

                            <div class="o_row">
                                <label for="mark_as_packing" string="Packing Service"/>
                                <field name="mark_as_packing" widget="boolean_toggle" nolabel="1"/>
                            </div>
                        </group>
                        <group string="Summary">
                            <field name="package_count"/>
                            <field name="total_weight" class="fw-bold"/>
                            <field name="total_volume"/>
                            <field name="total_value" class="fw-bold text-primary"/>
                        </group>
                    </group>

                    <notebook>
                         <page string="Sender Information">
                            <group>
                                <group string="Contact Details">
                                    <field name="sender_id" options="{'no_create': False}"/>
                                    <field name="sender_name"/>
                                    <field name="sender_phone"/>
                                    <field name="sender_mobile"/>
                                    <field name="sender_whatsapp" widget="phone"
                                           placeholder="e.g. +201234567890"/>
                                    <field name="sender_email" widget="email"/>
                                </group>
                                <group string="Address">
                                    <field name="sender_address" widget="text"/>
                                    <field name="sender_city"/>
                                    <field name="sender_governorate_id"
                                           options="{'no_create': True, 'no_open': True}"
                                           required="1"/>
                                    <field name="sender_zip"/>
                                    <field name="sender_country_id" invisible="1"/>
                                </group>
                            </group>
                            <group>
                                <group string="Pickup Preferences">
                                    <field name="sender_preferred_pickup_time" widget="radio"
                                           options="{'horizontal': True}"/>
                                </group>
                            </group>

                             <group>
    <group string="Pickup Service">
        <field name="pickup_type" widget="radio" options="{'horizontal': True}"/>
        <field name="pickup_fee"
               widget="monetary"
               invisible="pickup_type != 'customer'"

               force_save="1"
               help="Pickup fee based on customer category"/>
        <div class="text-muted" invisible="pickup_type != 'customer' or not sender_id">
            <span invisible="not sender_id.price_category_id">
                <i class="fa fa-info-circle"/>
                Fee applied based on category: <field name="customer_category_id" readonly="1" nolabel="1" class="fw-bold"/>
            </span>
        </div>
    </group>
</group>

                            <group string="Pickup Instructions">
                                <field name="sender_pickup_notes" nolabel="1"
                                       placeholder="e.g. Building 5, 3rd floor, ask for Mr. Ahmed at reception..."/>
                            </group>
                        </page>

                        <page string="Recipient Information">
                            <group>
                                <group string="Contact Details">
                                    <field name="recipient_id" options="{'no_create': False}"/>
                                    <field name="recipient_name"/>
                                    <field name="recipient_phone"/>
                                    <field name="recipient_mobile"/>
                                    <field name="recipient_whatsapp" widget="phone"
                                           placeholder="e.g. +201234567890"/>
                                    <field name="recipient_alternative_phone" widget="phone"/>
                                    <field name="recipient_email" widget="email"/>
                                </group>
                                <group string="Address">
                                    <field name="recipient_address" widget="text"/>
                                    <field name="recipient_city"/>
                                    <field name="recipient_governorate_id"
                                           options="{'no_create': True, 'no_open': True}"
                                           required="1"/>
                                    <field name="recipient_zip"/>
                                    <field name="recipient_country_id" invisible="1"/>
                                </group>
                            </group>
                            <group>
                                <group string="Delivery Preferences">
                                    <field name="recipient_preferred_delivery_time" widget="radio"
                                           options="{'horizontal': True}"/>
                                </group>
                            </group>
                            <group string="Delivery Instructions">
                                <field name="recipient_delivery_notes" nolabel="1"
                                       placeholder="e.g. Villa 25, leave with security if not home, call before delivery..."/>
                            </group>
                        </page>

                        <page string="Products">

                                <field name="shipment_line_ids">
                                <list editable="bottom">
                                    <field name="product_name"/>
                                    <field name="category_id"/>
                                    <field name="subcategory_id"
                                           domain="[('category_id', '=', category_id)]"
                                           optional="show"/>
                                    <field name="brand_id" optional="show"/>
                                    <field name="quantity"/>
                                    <field name="weight" sum="Total Weight"/>

                                    <field name="product_value" sum="Total Value"/>

                                    <field name="length"/>
                                    <field name="width"/>
                                    <field name="height"/>
                                    <field name="volume" sum="Total Volume"/>
                                    <field name="hs_code" optional="show"/>
                                    <field name="serial_number" optional="show"/>
                                    <field name="notes" optional="show"/>
                                    <field name="fragile" widget="boolean_toggle" optional="show"/>
                                    <field name="dangerous_goods" widget="boolean_toggle" optional="show"/>
                                </list>
                            </field>


                            <group class="oe_subtotal_footer oe_right">
                                <field name="total_weight" class="oe_subtotal_footer_separator"/>
                                <field name="total_value"/>
                            </group>
                        </page>




                         <page string="Additional Services" name="additional_services">
                           <group string="Select Additional Services" invisible="not customer_category_id">
                                <field name="additional_service_ids" nolabel="1">
                                    <list editable="bottom">
                                        <field name="service_id"
                                               options="{'no_create': True}"
                                               domain="[('active', '=', True)]"/>
                                        <field name="fee_amount"
                                               widget="monetary"
                                               sum="Total"
                                               readonly="0"
                                               force_save="1"/>
                                    </list>
                                </field>
                            </group>

                            <div class="alert alert-warning" invisible="customer_category_id">
                                <i class="fa fa-warning"/> Please select a customer first to add additional services
                            </div>
                        </page>

                        <page string="Notes">
                            <group>
                                <field name="notes" placeholder="Customer notes..." nolabel="1"/>
                                <field name="internal_notes" placeholder="Internal notes (not visible to customer)..."
                                       nolabel="1" groups="base.group_user"/>
                            </group>
                        </page>


                        <page string="Pricing &amp; Shipping">
                    <group>
                        <group string="Shipping Company">
                            <field name="shipping_company_id"
                                   options="{'no_create': True}"
                                   required="state == 'confirmed'"/>

                            <!-- زر إعادة الحساب -->
                            <button name="action_recalculate_fees"
                                    type="object"
                                    string="↻ Recalculate Fees"
                                    class="btn-link"
                                    invisible="not shipping_company_id or not recipient_governorate_id"
                                    help="Recalculate all shipping fees"/>
                        </group>



                        <group>
                        <group string="Payment &amp; Insurance">
                            <field name="payment_method" required="1"/>

                            <field name="prepaid_payer"
                                   invisible="payment_method != 'prepaid'"
                                   required="payment_method == 'prepaid'"
                                   widget="radio"
                                   options="{'horizontal': True}"/>

                            <div class="alert alert-info"
                                 invisible="payment_method != 'prepaid'"
                                 colspan="2">
                                <i class="fa fa-info-circle"/>
                                For prepaid shipments, select who will pay for the shipping:
                                <ul>
                                    <li><b>Sender:</b> Invoice will be issued to the sender</li>
                                    <li><b>Recipient:</b> Invoice will be issued to the recipient</li>
                                </ul>
                            </div>

                            <!-- عرض من سيصدر له الفاتورة -->
                            <field name="invoice_partner_id"
                                   readonly="1"
                                   invisible="not invoice_partner_id"
                                   string="Invoice will be issued to"/>

                            <!-- حقول COD الجديدة -->
                            <field name="cod_amount"
                                   widget="monetary"
                                   invisible="payment_method != 'cod'"
                                   required="payment_method == 'cod'"/>

                             <field name="cod_amount_sheet_excel"
                                   widget="monetary"
                                   invisible="payment_method != 'cod'"
                                   required="payment_method == 'cod'"/>

                            <field name="include_services_in_cod"  invisible="payment_method != 'cod'" />

                            <field name="cod_payment_type"
                                   invisible="payment_method != 'cod'"
                                   required="payment_method == 'cod'"
                                  />

                            <field name="cod_includes_shipping"
                                   invisible="payment_method != 'cod'"/>

                            <!-- حقول التأمين -->
                            <field name="insurance_required" widget="boolean_toggle"/>

                        </group>
                    </group>

                        <group string="Cost Breakdown" invisible="not shipping_company_id">
                            <field name="base_shipping_cost"
                                   widget="monetary"
                                   force_save="1"/>
                            <field name="weight_shipping_cost"
                                   widget="monetary"
                                   invisible="weight_shipping_cost == 0"
                                   force_save="1"/>


                            <field name="cod_fee_amount"
                                   widget="monetary"
                                   invisible="payment_method != 'cod'"
                                   force_save="1"/>
                            <field name="insurance_fee_amount"
                                   widget="monetary"
                                   invisible="not insurance_required"
                                   force_save="1"/>



                            <separator/>
                            <field name="shipping_cost"
                                   widget="monetary"
                                   class="oe_subtotal_footer_separator fw-bold text-primary"
                                   force_save="1"/>
                        </group>
                    </group>



                </page>

                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Tree View -->

    <!-- استبدل Tree View بالكامل -->
    <record id="view_shipment_order_list" model="ir.ui.view">
        <field name="name">shipment.order.list</field>
        <field name="model">shipment.order</field>
        <field name="priority">99</field>
        <field name="arch" type="xml">
            <list string="Shipments" editable="bottom" multi_edit="1">
                <!-- Header buttons -->
                <header>
                    <button name="action_export_to_excel"
                            string="Export to Excel"
                            type="object"
                            class="btn-primary"/>
                </header>

                <!-- Fields -->
                <field name="order_number"/>
                <field name="tracking_number" optional="show"/>
                <field name="sender_id"/>
                <field name="recipient_id"/>
                <field name="recipient_city" optional="show"/>
                <field name="package_count" optional="show"/>
                <field name="total_weight" sum="Total Weight"/>
                <field name="pickup_date"/>
                <field name="expected_delivery" optional="show"/>
                <field name="shipping_company_id" optional="show"/>
                <field name="tracking_number_shipping_company" optional="show"/>

                <!-- State field -->
                <field name="state"
                       widget="selection"
                       decoration-success="state == 'delivered'"
                       decoration-warning="state in ['out_for_delivery', 'returned']"
                       decoration-danger="state == 'cancelled'"
                       decoration-info="state == 'draft'"/>

            </list>
        </field>
    </record>




    <!-- Search View -->
    <record id="view_shipment_order_search" model="ir.ui.view">
        <field name="name">shipment.order.search</field>
        <field name="model">shipment.order</field>
        <field name="arch" type="xml">
            <search string="Search Shipments">
                <field name="order_number"/>
                <field name="sender_id"/>
                <field name="recipient_id"/>
                <field name="recipient_city"/>
                <field name="tracking_number"/>
                <field name="tracking_number_shipping_company"/>


                <separator/>
                <filter name="draft" string="Draft" domain="[('state','=','draft')]"/>
                <filter name="confirmed" string="Confirmed" domain="[('state','=','confirmed')]"/>
                <filter name="in_transit" string="In Transit"
                        domain="[('state','in',['picked','in_transit','out_for_delivery'])]"/>
                <filter name="delivered" string="Delivered" domain="[('state','=','delivered')]"/>
                <filter name="cancelled" string="Cancelled" domain="[('state','=','cancelled')]"/>
                <separator/>
                <filter name="insurance" string="Insured" domain="[('insurance_required','=',True)]"/>
                <filter name="cod" string="COD" domain="[('payment_method','=','cod')]"/>
                <separator/>
                <filter name="today_pickup" string="Today's Pickup"
                        domain="[('pickup_date', '&gt;=', context_today().strftime('%Y-%m-%d')),
                                ('pickup_date', '&lt;', (context_today() + relativedelta(days=1)).strftime('%Y-%m-%d'))]"/>
                <group expand="0" string="Group By">
                    <filter name="sender" string="Sender" context="{'group_by':'sender_id'}"/>
                    <filter name="recipient" string="Recipient" context="{'group_by':'recipient_id'}"/>
                    <filter name="shipping_company" string="Shipping Company"
                            context="{'group_by':'shipping_company_id'}"/>
                    <filter name="state" string="Status" context="{'group_by':'state'}"/>
                    <filter name="pickup_date" string="Pickup Date" context="{'group_by':'pickup_date'}"/>
                    <filter name="shipment_type" string="Type" context="{'group_by':'shipment_type'}"/>

                </group>
            </search>
        </field>
    </record>

    <!-- Graph View -->
    <record id="view_shipment_order_graph" model="ir.ui.view">
        <field name="name">shipment.order.graph</field>
        <field name="model">shipment.order</field>
        <field name="arch" type="xml">
            <graph string="Shipment Analytics" type="bar">
                <field name="state" type="row"/>

                <field name="total_weight" type="measure"/>
            </graph>
        </field>
    </record>

    <!-- Pivot View -->
    <record id="view_shipment_order_pivot" model="ir.ui.view">
        <field name="name">shipment.order.pivot</field>
        <field name="model">shipment.order</field>
        <field name="arch" type="xml">
            <pivot string="Shipment Analysis">
                <field name="sender_id" type="row"/>
                <field name="state" type="col"/>


                <field name="total_weight" type="measure"/>
                <field name="package_count" type="measure"/>
            </pivot>
        </field>
    </record>

    <!-- Calendar View -->
    <record id="view_shipment_order_calendar" model="ir.ui.view">
        <field name="name">shipment.order.calendar</field>
        <field name="model">shipment.order</field>
        <field name="arch" type="xml">
            <calendar string="Shipments Calendar" date_start="pickup_date"
                     date_stop="expected_delivery" color="shipping_company_id"
                     quick_create="0">
                <field name="order_number"/>
                <field name="sender_id"/>
                <field name="recipient_id"/>
                <field name="state"/>
            </calendar>
        </field>
    </record>

    <!-- Action -->
    <record id="action_shipment_order" model="ir.actions.act_window">
    <field name="name">Shipment Orders</field>
    <field name="res_model">shipment.order</field>
    <!-- لو إصدارك قبل 16 استخدم tree بدل list -->
    <field name="view_mode">list,kanban,form,graph,pivot,calendar</field>
    <field name="views" eval="[
        (ref('shipping_management_system.view_shipment_order_list'), 'list'),
        (False, 'kanban'),
        (False, 'form'),
        (False, 'graph'),
        (False, 'pivot'),
        (False, 'calendar')
    ]"/>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">Create your first shipment order</p>
        <p>Manage your shipping operations efficiently with our comprehensive shipment tracking system.</p>
    </field>
</record>

    <!-- Menu Items -->


    <!-- Subcategory Views -->
    <record id="view_product_subcategory_list" model="ir.ui.view">
        <field name="name">product.subcategory.list</field>
        <field name="model">product.subcategory</field>
        <field name="arch" type="xml">
            <list string="Subcategories" editable="bottom">
                <field name="name"/>
                <field name="category_id"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="action_product_subcategory" model="ir.actions.act_window">
        <field name="name">Subcategories</field>
        <field name="res_model">product.subcategory</field>
        <field name="view_mode">list</field>
    </record>



    <!-- Brand Views -->
    <record id="view_product_brand_list" model="ir.ui.view">
        <field name="name">product.brand.list</field>
        <field name="model">product.brand</field>
        <field name="arch" type="xml">
            <list string="Brands" editable="bottom">
                <field name="name"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="action_product_brand" model="ir.actions.act_window">
        <field name="name">Brands</field>
        <field name="res_model">product.brand</field>
        <field name="view_mode">list</field>
    </record>



    <record id="view_shipment_order_form_governorate_update" model="ir.ui.view">
        <field name="name">shipment.order.form.governorate.update</field>
        <field name="model">shipment.order</field>
        <field name="inherit_id" ref="shipping_management_system.view_shipment_order_form"/>
        <field name="arch" type="xml">
            <!-- إخفاء الحقول القديمة -->
            <xpath expr="//field[@name='recipient_governorate_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
            <xpath expr="//field[@name='sender_governorate_id']" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>

        </field>
    </record>
    

</odoo>