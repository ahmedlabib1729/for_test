// test/widget_test.dart
// ملف اختبار أساسي للتطبيق

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

// استخدم اسم الـ package الصحيح من pubspec.yaml
import 'package:login_app/main.dart';

void main() {
  testWidgets('App loads correctly', (WidgetTester tester) async {
    // هذا اختبار بسيط للتأكد من أن التطبيق يعمل
    // يمكنك إضافة اختبارات أكثر تفصيلاً لاحقاً

    // ملاحظة: قد تحتاج لتعديل هذا الاختبار حسب متطلباتك
    expect(true, isTrue);
  });
}